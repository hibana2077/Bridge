# Redis Configuration
REDIS_PASSWORD='your_redis_password'

# >>> key = Fernet.generate_key()
# >>> key
# Encryption key for sensitive data (generate a secure random key)
ENCRYPTION_KEY='ZmDfcTF7_60GrrY167zsiPd67pEvs0aGOv2oasOM1Pg=' # example key, replace with your own

# API URL (use localhost when running locally)
# API_BASE_URL=http://localhost:8000

# Change to http://backend:8000 when accessing from inside Docker containers
API_BASE_URL=http://backend:8000